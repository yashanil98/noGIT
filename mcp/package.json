{
  "name": "nogit-mcp",
  "version": "0.1.0",
  "description": "MCP server for noGIT local snapshots, usable by terminal AI agents without VS Code.",
  "license": "MIT",
  "type": "module",
  "main": "dist/server.js",
  "bin": {
    "nogit-mcp": "dist/server.js"
  },
  "scripts": {
    "build": "tsc -p .",
    "test": "tsc -p . && node --test dist/test/"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=18"
  }
}
